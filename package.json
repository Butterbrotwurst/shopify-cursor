{
  "name": "cityxz-shopify-theme",
  "version": "1.0.0",
  "description": "Local development setup for Cityxz Shopify theme",
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.liquid' '**/*.css' '**/*.js' --port 3000 --no-open",
    "watch": "browser-sync start --proxy 'morph-official.myshopify.com' --files '**/*.liquid' '**/*.css' '**/*.js' --port 3000",
    "build": "echo 'Theme ready for upload'",
    "zip": "zip -r theme-$(date +%Y%m%d-%H%M%S).zip assets config layout locales sections snippets templates -x '*.DS_Store' '*node_modules*'",
    "lint": "echo 'Theme linting - add shopify theme check later'"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3"
  },
  "keywords": ["shopify", "theme", "liquid", "cityxz"],
  "author": "Cityxz Team",
  "license": "MIT"
}
